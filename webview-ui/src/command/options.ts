export enum ReceiveOptions {
    setPage = "setPage",
    openConnWindow = "openConnWindow",
    dbConnection = "dbConnection",
}

export enum PostOptions {
    clearConnectionEvent = "clear-connection",
    createConnection = "create-connection"
}